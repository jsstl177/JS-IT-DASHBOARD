# Server Configuration
PORT=5000
NODE_ENV=development

# Authentication
# REQUIRED: Set a strong password for the admin user
DEFAULT_ADMIN_PASSWORD=changeme

# REQUIRED: Set a strong random secret for JWT token signing (min 32 characters)
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=

# REQUIRED: Set a strong random key for encrypting credentials at rest (min 32 characters)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=

# CORS (comma-separated origins for production)
# ALLOWED_ORIGINS=https://yourdomain.com

# Database
DATABASE_PATH=./server/dashboard.db
