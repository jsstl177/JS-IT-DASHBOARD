# Server Configuration
PORT=5000
NODE_ENV=development

# Authentication
# REQUIRED: Set a strong password for the admin user
DEFAULT_ADMIN_PASSWORD=changeme

# REQUIRED: Set a strong random secret for JWT token signing (min 32 characters)
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=

# REQUIRED: Set a strong random key for encrypting credentials at rest (min 32 characters)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=

# CORS (comma-separated origins for production)
# ALLOWED_ORIGINS=https://yourdomain.com

# Database
DATABASE_PATH=./server/dashboard.db

# Proxmox VE API Configuration (configured via Settings UI)
# Use the Dashboard Settings UI to configure Proxmox with API Token authentication
# Required fields in Settings:
#   - Base URL: https://192.168.177.89:8006 (your Proxmox host)
#   - API Key (Token ID): USER@REALM!TOKENID (e.g., root@pam!dashboard)
#   - API Secret: The UUID token secret generated in Proxmox
#
# To create an API Token in Proxmox:
# 1. Go to Datacenter → Permissions → API Tokens
# 2. Click Add → Select user (e.g., root@pam)
# 3. Enter Token ID (e.g., "dashboard")
# 4. Set expiration if needed, uncheck "Privilege Separation" for full access
# 5. Copy the Secret UUID shown after creation (this is the only time it's shown!)
